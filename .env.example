# Obtain a bot token from @BotFather. Never commit real credentials.
TELEGRAM_BOT_TOKEN=
# Your numeric Telegram user ID; other users and group chats are rejected.
TELEGRAM_OWNER_ID=
# OpenAI-compatible Chat Completions endpoint and model with JSON schema support.
LLM_BASE_URL=https://api.openai.com/v1
LLM_API_KEY=
LLM_MODEL=
# schema (recommended), json, or prompt for compatible providers.
LLM_JSON_MODE=schema
TIMEZONE=Europe/Moscow
LESSON_TIMES=10:00,14:00,19:00
DATABASE_PATH=data/tutor.sqlite3
