services:
  tutor:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_PATH: /app/data/tutor.sqlite3
    volumes:
      - tutor-data:/app/data
    stop_grace_period: 30s

volumes:
  tutor-data:
